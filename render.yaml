# Render — Static Site
# https://render.com/docs/static-sites
services:
  - type: web
    name: wlan-guia-epn-2026a
    runtime: static
    staticPublishPath: .
    buildCommand: echo "Portal WLAN EPN 2026-A — sitio estatico listo"
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=600
